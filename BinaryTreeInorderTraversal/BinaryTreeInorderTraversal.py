class TreeNode(object):
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

def inorderTraversal(root):
        """
        :type root: TreeNode
        :rtype: List[int]
        """
        res = []

        def createTreeNode(v, l, r):
            t = TreeNode(root[i], root[i+1], root[i+2]) 
        for i in range(len(root)):    
            if not t.val:
                 return
            
                 
             



inorderTraversal([1,None,2,3])
inorderTraversal([])
inorderTraversal([1])
